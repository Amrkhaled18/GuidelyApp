services:
  - type: web
    name: GuidelyApp
    runtime: docker
    repo: https://github.com/Amrkhaled18/GuidelyApp
    dockerfilePath: FinalProjectTest/Dockerfile  # <- path to Dockerfile
    plan: free
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: OpenAI__ApiKey
        value: "your-real-openai-key"
