services:
  - type: web
    name: GuidelyApp
    runtime: docker
    plan: free
    buildCommand: |
      cd ClientApp
      npm install
      npm run build
      cd ..
      dotnet publish FinalProjectTest.csproj -c Release -o out
    startCommand: dotnet out/FinalProjectTest.dll
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
      - key: OpenAI__ApiKey
        value: ""
